apiVersion: 1

providers:
  - name: 'FOReporting Dashboards'
    orgId: 1
    folder: 'FOReporting'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards