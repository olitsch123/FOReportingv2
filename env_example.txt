# FOReporting v2 Environment Configuration
# SECRETS ONLY - All other settings in config/runtime.yaml
# Copy this file to .env and fill in your actual values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL database settings
POSTGRES_DB=your_database_name
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_secure_password

# Database connection URLs
# For Docker deployment (using container name):
DATABASE_URL=postgresql+psycopg://your_username:your_secure_password@postgres:5432/your_database_name
# For local development (using localhost):
# DATABASE_URL=postgresql+psycopg://your_username:your_secure_password@localhost:5432/your_database_name

# =============================================================================
# OPENAI CONFIGURATION (REQUIRED)
# =============================================================================
# Get your OpenAI API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-placeholder-replace-with-your-actual-key

# Get your Vector Store ID from OpenAI or create one
# You can create one at: https://platform.openai.com/vector-stores
OPENAI_VECTOR_STORE_ID=vs_placeholder_replace_with_your_actual_id

# =============================================================================
# INVESTOR FOLDER PATHS
# =============================================================================
# Paths to investor document folders (OS-specific)
# These paths will be monitored for new documents

# Investor 1 folder (adjust for your OS and structure)
# Windows: C:\Users\YourUser\Documents\Investor1\Funds
# Linux/Mac: /home/youruser/documents/investor1/funds
INVESTOR1_PATH=path/to/your/investor1/documents/folder

# Investor 2 folder
INVESTOR2_PATH=path/to/your/investor2/documents/folder

# Docker volume mappings (for Docker deployment)
# These should match your host paths for volume mounting
INVESTOR1_PATH_HOST=path/to/your/investor1/documents/folder
INVESTOR2_PATH_HOST=path/to/your/investor2/documents/folder

# =============================================================================
# VECTOR STORE CONFIGURATION
# =============================================================================
# ChromaDB storage directory
# For local development:
CHROMA_DIR=./data/chroma
# For Docker deployment:
# CHROMA_DIR=/app/data/chroma

# =============================================================================
# WINDOWS UTF-8 SETTINGS (REQUIRED)
# =============================================================================
# Force UTF-8 encoding on Windows
PGCLIENTENCODING=UTF8
PYTHONUTF8=1

# =============================================================================
# OPTIONAL DEVELOPMENT SETTINGS
# =============================================================================
# Uncomment these for development/debugging
# DEBUG=true
# LOG_LEVEL=DEBUG
# DISABLE_AUTH=true

# =============================================================================
# DEPLOYMENT MODE SELECTION
# =============================================================================
# Set this to control which configuration set to use
# Values: local | docker | production
DEPLOYMENT_MODE=local